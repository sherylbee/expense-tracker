{
    "version": "0.2.0",
    //NOTE @SPB TODO: npm run start to run program FIRST; THEN launch debugger to observe already-running process... wow. what a PITA.
    "configurations": [
        {
            "name": "Launch Chrome",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}",
        }
    ]
}